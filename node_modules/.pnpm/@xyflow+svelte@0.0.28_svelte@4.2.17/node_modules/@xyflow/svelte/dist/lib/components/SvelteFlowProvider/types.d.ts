import type { Edge, Node } from '../../types';
export type SvelteFlowProviderProps = {
    initialNodes?: Node[];
    initialEdges?: Edge[];
    initialWidth?: number;
    initialHeight?: number;
    fitView?: boolean;
};
