/// <reference types="svelte" />
import type { Node } from '../types';
import type { SvelteFlowStoreState } from './types';
export declare function getVisibleNodes(store: SvelteFlowStoreState): import("svelte/store").Readable<Node[]>;
